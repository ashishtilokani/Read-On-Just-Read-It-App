<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".Sentence" 
    android:background="@drawable/notebookpaper"
   >

    <EditText
        android:id="@+id/sentenceEditText"
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:hint="Enter a Sentence"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:layout_toLeftOf="@+id/sentenceAdd"
        android:ems="10" >

        <requestFocus />
    </EditText>

    <ListView
        android:id="@+id/sentenceList"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/sentenceEditText"
        android:layout_alignLeft="@+id/sentenceEditText"
        android:layout_alignParentTop="true" >

    </ListView>

    <ImageButton
        android:id="@+id/sentenceAdd"
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:layout_alignParentRight="true"
        android:layout_alignTop="@+id/sentenceEditText"
        android:src="@android:drawable/ic_input_add" />

</RelativeLayout>
